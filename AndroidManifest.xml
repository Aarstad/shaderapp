<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dev.aarstad.shader"
    android:versionCode="12"
    android:versionName="2.5">

  <uses-feature android:glEsVersion="0x00020000" android:required="false" />

  <!-- Front-loaded on purpose.

       A push can change code but never the manifest, so a permission that
       isn't declared here costs a reinstall the day a plugin wants it. None of
       these are granted by being listed: the install prompt shows the dangerous
       ones, and they still have to be requested at runtime and approved before
       they do anything. Declaring them only means a plugin *can* ask.

       The dangerous ones below are commented out, which is the opposite of
       front-loading and deliberate. Declaring them made this build refuse to
       install on MagicOS. A sideload that suddenly wants camera, microphone
       and media access is exactly what a device-side risk scan exists to stop.
       An app you cannot install is worse than one that needs a reinstall the
       day it wants a camera, so they wait until something actually needs them.

       Uncomment, bump versionCode, reinstall. -->

  <uses-permission android:name="android.permission.INTERNET" />
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
  <uses-permission android:name="android.permission.WAKE_LOCK" />
  <uses-permission android:name="android.permission.VIBRATE" />
  <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
  <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

  <!--
  <uses-permission android:name="android.permission.CAMERA" />
  <uses-permission android:name="android.permission.RECORD_AUDIO" />
  <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
  <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
  <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
  -->

  <application
      android:label="@string/app_name"
      android:hasCode="true">

    <!-- The launcher. Point this at .host.HostActivity and delete src/gl to
         get the bare template, with nothing but a plugin container. -->
    <activity
        android:name=".gl.ShaderActivity"
        android:exported="true"
        android:configChanges="orientation|screenSize|keyboardHidden|screenLayout">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <!-- A spare screen, declared before anything needs it. Activities cannot
         be added by a push, so a plugin that wants a second screen needs this
         to already exist. -->
    <activity
        android:name=".host.PluginScreen"
        android:exported="false"
        android:configChanges="orientation|screenSize|keyboardHidden|screenLayout" />

    <!-- Likewise for carrying on with the app off screen. -->
    <service
        android:name=".host.PluginService"
        android:exported="false"
        android:foregroundServiceType="dataSync" />

  </application>
</manifest>
